---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../../consts';
import { getTagCounts, tagToSlug } from '../../utils/posts';

const posts = await getCollection('blog');
const tags = getTagCounts(posts);
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={`标签 | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<div class="site-bg"></div>
		<Header />
		<main>
			<section class="page-heading glass-panel">
				<p class="eyebrow">主题索引</p>
				<h1>标签</h1>
				<p>按主题快速找到算法、论文、项目和站点记录。</p>
			</section>
			<section class="tag-cloud">
				{
					tags.map(([tag, count]) => (
						<a href={`/tags/${tagToSlug(tag)}/`}>
							<span>{tag}</span>
							<strong>{count}</strong>
						</a>
					))
				}
			</section>
		</main>
		<Footer />
	</body>
</html>
